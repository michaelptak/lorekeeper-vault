---
note_purpose: Documentation
---
This is a seed note. Its purpose is to hold metadata to improve suggestions for an empty vault.

If it ends up in a base/index, make sure to filter out the templates path. `!file.path.contains("Templates")`